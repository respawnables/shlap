<nav class="navbar navbar-expand-lg navbar-dark bg-secondary ms-auto">
  <div class="container">
    <%= link_to 'Shlap', root_path, id: "logo", class: "navbar-brand" %>
    <% if current_user %>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Shlaps
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to 'View all Shlaps', titles_path, class: "dropdown-item" %>
              <%= link_to 'Create Shlap', new_title_path, class: "dropdown-item" %>
            </ul>
          </li>
        </ul>
    <% end %>
    <ul class="navbar-nav ms-auto">
      <% if logged_in? %>
        <%= link_to 'Profile', user_path(current_user), class: "text-white mt-1" %>
        <li class="nav-item">
          <%= link_to 'Logout', logout_path(current_user), class: "ms-3 btn btn-outline-warning", method: :delete %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Login', login_path, class: "btn btn-warning" %>
        </li>
      <% end %>
    </ul>
    </div>

  </div>
</nav>